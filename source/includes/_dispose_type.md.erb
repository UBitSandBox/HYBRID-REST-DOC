# <%= t('archive.disposeType.title') %>
## <%= t('archive.disposeType.object.title') %>
<%= t('table.name') %> | <%= t('table.type') %> | <%= t('table.description') %>
--------- | ----------- | -----------
id | integer |  <%= t('archive.disposeType.object.table.id') %>
title | string | <%= t('archive.disposeType.object.table.title') %>
titleF | string | <%= t('archive.disposeType.object.table.titleF') %>
titleI | string | <%= t('archive.disposeType.object.table.titleI') %>
titleE | string | <%= t('archive.disposeType.object.table.titleE') %>

## <%= t('archive.disposeType.getAll.title') %>
```shell
curl --request GET \
  --url http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types \
  --header 'Authorization: Bearer {ACCESS_TOKEN}'
```

```javascript--node
```

><%= t('message.replaceToken') %>

><%= t('message.returnJson') %>

```json
[
  {
    "id": 1,
    "title": "Noch nicht bewertet",
    "titleF": "Pas encore évalué",
    "titleI": "Non ancora valutato",
    "titleE": "Not evaluated"
  },
  {
    "id": 2,
    "title": "Bundesarchiv",
    "titleF": "Archives fédérales",
    "titleI": "Archivio federale",
    "titleE": "Transfer"
  }
]
```

<%= t('archive.disposeType.getAll.description') %>

### <%= t('httpRequest') %>

`GET http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types`

<aside class="success">
<%= t('message.remember') %>
</aside>

## <%= t('archive.disposeType.getById.title') %>

```shell
curl --request GET \
  --url http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/1 \
  --header 'Authorization: Bearer {ACCESS_TOKEN}'
```

```javascript--node
```

><%= t('message.replaceToken') %>

><%= t('message.returnJson') %>

```json
{
    "id": 1,
    "title": "Noch nicht bewertet",
    "titleF": "Pas encore évalué",
    "titleI": "Non ancora valutato",
    "titleE": "Not evaluated"
}
```

<%= t('archive.disposeType.getById.description') %>

### <%= t('httpRequest') %>
`GET http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/<ID>`

### <%= t('urlParameters') %>
<%= t('table.parameter') %> | <%= t('table.description') %>
--------- | -----------
ID | <%= t('archive.disposeType.getById.table.id') %>

<aside class="success">
<%= t('message.remember') %>
</aside>

## <%= t('archive.disposeType.create.title') %>
```shell
curl --request POST \
  --url http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/ \
  --header 'Authorization: Bearer {ACCESS_TOKEN}' \
  --data '{"title": "Nicht archivwürdig"}'
```

```javascript--node
```

><%= t('message.replaceToken') %>

<%= t('archive.disposeType.create.description') %>

### <%= t('httpRequest') %>
`POST http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/`

### <%= t('bodyParameters') %>
<%= t('table.parameter') %> | <%= t('table.type') %> | <%= t('table.description') %>
--------- | ----------- | -----------
title * | string | <%= t('archive.disposeType.object.table.title') %>
titleF | string | <%= t('archive.disposeType.object.table.titleF') %>
titleI | string | <%= t('archive.disposeType.object.table.titleI') %>
titleE | string | <%= t('archive.disposeType.object.table.titleE') %>

<aside class="notice">
<%= t('message.asterisk') %>
</aside>

<aside class="success">
<%= t('message.remember') %>
</aside>

## <%= t('archive.disposeType.update.title') %>
```shell
curl --request PUT \
  --url http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/3 \
  --header 'Authorization: Bearer {ACCESS_TOKEN}' \
  --data '{"titleF": "Sans valeur archivistique"}'
```

```javascript--node
```

><%= t('message.replaceToken') %>

<%= t('archive.disposeType.update.description') %>

### <%= t('httpRequest') %>
`PUT http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/<ID>`

### <%= t('urlParameters') %>
<%= t('table.parameter') %> | <%= t('table.description') %>
--------- | -----------
ID | <%= t('archive.disposeType.update.table.id') %>

### <%= t('bodyParameters') %>
<%= t('table.parameter') %> | <%= t('table.type') %> | <%= t('table.description') %>
--------- | ----------- | -----------
title | string | <%= t('archive.disposeType.object.table.title') %>
titleF | string | <%= t('archive.disposeType.object.table.titleF') %>
titleI | string | <%= t('archive.disposeType.object.table.titleI') %>
titleE | string | <%= t('archive.disposeType.object.table.titleE') %>

<aside class="success">
<%= t('message.remember') %>
</aside>

## <%= t('archive.disposeType.delete.title') %>
```shell
curl --request DELETE \
  --url http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/1 \
  --header 'Authorization: Bearer {ACCESS_TOKEN}'
```

```javascript--node
```
><%= t('message.replaceToken') %>

<%= t('archive.disposeType.delete.description') %>

### <%= t('httpRequest') %>
`DELETE http://<%= t('ip') %>/api/v1/ubit/archive/dispose_types/<ID>`

### <%= t('urlParameters') %>
<%= t('table.parameter') %> | <%= t('table.description') %>
--------- | -----------
ID | <%= t('archive.disposeType.delete.table.id') %>

<aside class="success">
<%= t('message.remember') %>
</aside>
